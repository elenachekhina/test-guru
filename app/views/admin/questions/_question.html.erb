<tr>
  <td><%= link_to question.body, admin_question_path(question) %></td>
  <td><%= button_to 'Edit', edit_admin_question_path(question), method: :get %></td>
  <td><%= button_to 'Delete', admin_question_path(question), method: :delete, form: { data: { confirm: 'Are you sure?' } } %></td>
</tr>
